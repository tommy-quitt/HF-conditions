{
  "name": "hf-conditions",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "typecheck": "tsc --build --force",
    "lint": "eslint .",
    "collect": "tsx scripts/collect.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
